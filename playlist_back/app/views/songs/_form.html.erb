<%= form_with(model: song, local: true) do |form| %>
  <% if song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :song_name %>
    <%= form.text_field :song_name, id: :song_song_name %>
  </div>

  <div class="field">
    <%= form.label :singer_id %>
    <%= form.select :singer_id, Singer.all.collect{|singer| [singer.singer_name, singer.id]} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
